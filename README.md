# Demonstration of using kpt with jk

This shows:

 - constructing an image with jk scripts
 - using that image with `kpt fn`
 - parameterising it to use with `kpt cfg`
 - importing that into a configuration and using it
